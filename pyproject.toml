[project]
name = "metatag-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests",
    "beautifulsoup4",
    "lxml",
    "playwright>=1.58.0",
    "openpyxl",
    "tqdm",
]
